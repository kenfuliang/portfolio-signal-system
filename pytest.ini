[pytest]
# put the repo root on sys.path so `import strategy` works under bare `pytest`
# (CI runs `pytest`, not `python -m pytest` which would add cwd implicitly).
pythonpath = .
testpaths = tests
norecursedirs = backtests data reports .git .venv __pycache__
addopts = -q
